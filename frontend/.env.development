# Dev default: proxy /api/* to the live public API so `npm run dev` works out of
# the box without a local backend. Override in .env.local to point at a local
# api (e.g. FUNDING_API_INTERNAL_BASE_URL=http://localhost:8000).
FUNDING_API_INTERNAL_BASE_URL=https://api.quantshark.space
